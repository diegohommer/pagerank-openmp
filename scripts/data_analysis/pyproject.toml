[project]
name = "data-analysis"
version = "0.1.0"
description = "Directory with scripts for analysing and plotting data obtained from benchmarks."
requires-python = ">=3.12"
dependencies = [
    "matplotlib>=3.10.6",
    "notebook>=7.4.7",
    "pandas>=2.3.2",
]
